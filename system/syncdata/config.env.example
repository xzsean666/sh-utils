# Copy this file to config.env and adjust values.

# Jobs to execute. Names must be letters/numbers/underscores.
BACKUP_JOBS=(S3_PHOTOS SSH_DOCS PRECONFIG_REMOTE)

# Optional global rclone flags (applied to all jobs)
# RCLONE_GLOBAL_FLAGS="--transfers 4 --checkers 8"

# --- Job: upload to S3-compatible storage (Cloudflare R2 shown) ---
S3_PHOTOS_TYPE=S3
S3_PHOTOS_SRC="/data/photos"
S3_PHOTOS_S3_PROVIDER=Cloudflare
S3_PHOTOS_S3_ENDPOINT="https://<account_id>.r2.cloudflarestorage.com"
S3_PHOTOS_S3_BUCKET="my-bucket"
S3_PHOTOS_S3_PATH="photos"
S3_PHOTOS_S3_REGION="auto"
S3_PHOTOS_S3_ACCESS_KEY="your-access-key"
S3_PHOTOS_S3_SECRET_KEY="your-secret-key"
# S3_PHOTOS_S3_STORAGE_CLASS="STANDARD"
# S3_PHOTOS_S3_ACL="private"
S3_PHOTOS_DELETE=true
# S3_PHOTOS_RCLONE_FLAGS="--s3-chunk-size 64M"

# --- Job: backup to a remote server directory over SSH/SFTP ---
SSH_DOCS_TYPE=SSH
SSH_DOCS_SRC="/data/docs"
SSH_DOCS_SSH_HOST="backup.example.com"
SSH_DOCS_SSH_USER="root"
SSH_DOCS_SSH_PORT=22
SSH_DOCS_SSH_PATH="/srv/backups/docs"
# SSH_DOCS_SSH_KEY_FILE="~/.ssh/id_ed25519"
SSH_DOCS_DELETE=false

# --- Job: use a pre-configured rclone remote ---
PRECONFIG_REMOTE_TYPE=REMOTE
PRECONFIG_REMOTE_SRC="/data/misc"
PRECONFIG_REMOTE_DESTINATION="my-remote:/backups/misc"
PRECONFIG_REMOTE_DELETE=true
