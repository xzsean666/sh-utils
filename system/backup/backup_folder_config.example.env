# ==============================================================================
# Backup Configuration File Example
# ==============================================================================
# Usage: ./backup_folder.sh --config /path/to/this/config.env

# 1. Directory to backup (Source)
# Absolute path to the folder you want to backup
SOURCE_DIR="/home/sean/git/my-project"

# SLACK_WEBHOOK_URL=

# 2. Directory to store backups (Destination Root)
# The script will create a subdirectory named "$(basename SOURCE_DIR)_backup" here.
BACKUP_ROOT_DIR="/home/sean/backups"

# 3. Maximum number of backups to keep
MAX_BACKUPS=5

# 4. Compression Level (1-19)
# 1 is fastest, 19 is best compression (but slow).
# 3 is the default and offers a good balance of speed and size.
COMPRESSION_LEVEL=3

# 5. Exclude list (Bash Array Syntax)
# Patterns to exclude from the backup.
# Note: Paths are relative to the parent of SOURCE_DIR because the script uses -C
# However, simple names like "node_modules" work for any directory with that name.
# EXCLUDE_PARAMS=(
#     "--exclude=.git"           # Exclude git history
#     "--exclude=node_modules"   # Exclude node_modules in any subdirectory
#     "--exclude=*.log"          # Exclude log files
#     "--exclude=dist"           # Exclude build artifacts
#     "--exclude=.DS_Store"      # Exclude macOS system files
#     "--exclude=tmp"            # Exclude 'tmp' folders anywhere
#     "--exclude=backtest_results"            # Exclude 'tmp' folders anywhere
#     # "--exclude=secret_config.json" # Example of excluding a specific file
# )
EXCLUDE_PARAMS=(
    "--exclude=node_modules"   # Exclude node_modules in any subdirectory
    "--exclude=tmp"            # Exclude 'tmp' folders anywhere
    "--exclude=backtest_results"            # Exclude 'tmp' folders anywhere
)
